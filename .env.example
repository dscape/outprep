# Outprep — environment variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ─── Vercel Blob Storage ────────────────────────────────────────────────────
# Required for uploading FIDE player data (fide-pipeline upload commands).
# Not needed for local development — the app falls back to local files.
#
# Get a token from: Vercel Dashboard → Storage → Blob → Tokens
# Docs: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=
