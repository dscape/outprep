export default function UpgradeProgressBar() {
  // ErrorProfileCard already has a built-in progress bar with
  // "Analyzing... X/Y games", percentage, and cancel button.
  // This component was a duplicate indicator — removed.
  return null;
}
